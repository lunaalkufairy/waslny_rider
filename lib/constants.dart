import 'package:flutter/material.dart';

const Color green = Color(0xFF13C45E);
const Color blue = Color(0xFF2949FA);
const Color red = Color(0xFFF90000);
const Color yellow = Color(0xFFFDF239);
const Color grey3 = Color(0xFFF4F4F4);

/*
--------------------------------------------------------------------------------
                                Dark Mode
--------------------------------------------------------------------------------
*/
const Color black1 = Color(0xFF121212); // Trip info card, Select trip level,
const Color black = Color(
    0xFF0B0B0C); //background color of the phone number and OTP text field, search text field  background color,
const Color lightBlack = Color(
    0xFF252328); //Selection field, settings button, شروط والأحكام سياسة الخصوصية,arrow button

const Color grey = Color(0xFF413D48); //otp text field border color.

/*
--------------------------------------------------------------------------------
                                Light Mode
--------------------------------------------------------------------------------
*/
const Color lightGrey =
    Color(0xFFE4E4E7); //trip info card, location trip, map places, copon,
const Color white = Color(
    0xFFFFFFFF); //background color of the phone number and OTP and selection text fields, text field background, settings button, شروط والأحكام سياسة الخصوصية, arrows and search text field  background color,ordinary text in DARK MODE

/*
--------------------------------------------------------------------------------
                                Common
--------------------------------------------------------------------------------
*/
const Color lightWhite = Color(
    0xFFF5F2FB); // text field input and border in DARK MODE,text field border in LIGHT MODE,
const Color grey1 = Color(
    0xFF1E1E1E); //background color of text field, location trip, map places, copon , download button in DARK MODE, text field input and ordinary text in LIGHT MODE,
const Color lightWhite1 = Color(
    0xFFE1E1E1); //Phone number text field input and ordinary text in DARK MODE, trip info card, location trip, map places, copon, download button in LIGHT MODE
const Color grey2 = Color(0xFF9F9F9F);
const Color white1 = Color(0xFFF8FAFC);

const String baseUrl = 'https://waslny-project.vercel.app/api';
